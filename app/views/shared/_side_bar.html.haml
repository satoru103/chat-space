.side_bar
  .side_bar__Menu
    .side_bar__Menu__block
      %p.username
        = current_user.name
      %ul.icon
        %li.icon_cab
          =link_to new_group_path do
            =icon('fas', 'edit')
        %li.icon_cog
          =link_to edit_user_path(current_user) do
            =icon('fas', 'cog')
            
  .side_bar__group
    - current_user.groups.each do |group|
      .side_bar__group__block
        = link_to group_messages_path(group) do
          .group_name 
            = group.name  
          .group_message
            まだメッセージはありません
    
 

